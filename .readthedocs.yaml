version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11" # Updated to 3.11 to match your pyproject.toml requires-python

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

sphinx:
  configuration: docs/source/conf.py